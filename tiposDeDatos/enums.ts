(()=>{

    enum AudioLeven{

        min = 5,
        medium,
        medium2,
        max = 100
    }
    const currentAudio = AudioLeven.medium;

    console.log(currentAudio);
    console.log(AudioLeven);
})()